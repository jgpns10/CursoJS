// Método splice
const nomes = ['Maria', 'Eduarda', 'João', 'Gabriel', 'Mariana']

// nomes.splice(index, delete, elemento1, elemento2...);
// pop
/*
const removidos = nomes.splice(-1, 1);
console.log(nomes, removidos);
*/

// shift

// const removidos = nomes.splice(0, 1)
// console.log(nomes, removidos)

// push
// nomes.splice(nomes.length, 0, 'Junior');
// console.log(nomes, removidos)

// unshift
// nomes.splice(0, 0, 'Rafael', 'Juliana')