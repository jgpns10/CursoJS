const nome = 'João'; // string
const nome1 = "João"; // string
const nome2 = `João`;  // string
const num1 = 10; // number
const num2 = 10.52; // number
let nomeAluno; // undefined -> Não vai pra canto nenhum na memória
const sobrenomeAluno = null; // Nulo -> Não vai pra canto nenhum na memória, posso fazer alteerações futuras, diferente de undefined
const aprovado = true; // Boolean -> true, false (lógico)